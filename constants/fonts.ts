import { StyleSheet } from "react-native";

export const fonts = StyleSheet.create({
    button_text: {
        position: "absolute",
        fontWeight: "bold",
        fontSize: 25,
        color: "#333333",
    },
});
